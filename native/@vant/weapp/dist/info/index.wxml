<view class="custom-class van-info {{utils.bem( 'info',{dot:dot} )}}" style="{{customStyle}}" wx:if="{{info!==null&&info!==''||dot}}">{{dot?'':info}}</view>
<wxs module="utils" src="../wxs/utils.wxs" />